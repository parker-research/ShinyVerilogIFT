[package]
name = "shiny-verilog-ift"
version = "0.1.0"
edition = "2021"

[dependencies]
sv-parser = "0.13.3"

clap = { version = "4.5.31", features = ["derive"] }


[package.metadata.clippy]
deny = ["warnings"]
